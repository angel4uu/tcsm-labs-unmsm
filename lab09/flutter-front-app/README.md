# Flutter front-end skeleton for Lab09

This is a minimal Flutter MVVM-style skeleton that demonstrates login and persona search.

Notes:
- Update `ApiService.baseUrl` if your backend is not on the Android emulator (use `10.0.2.2:8000`).
- To run: `flutter pub get` then `flutter run`.
